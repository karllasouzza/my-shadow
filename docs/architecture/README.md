# Architecture Docs

This folder contains architecture notes for the Private Shadow Reflection Journal feature.

- Feature boundaries and MVVM decomposition
- `shared/` vs `features/` module layout
- Local AI gateway and `llama.rn` runtime integration

Add design diagrams and module responsibilities here.
