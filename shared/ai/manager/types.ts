/**
 * Re-exports from unified ../types for backwards compatibility.
 * All types are now defined in shared/ai/types.ts — this file exists
 * only so existing imports continue to work during the refactor.
 */

export type { DownloadedFile, DownloadState, ResumableRef } from "../types";

