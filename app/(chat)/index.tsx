/**
 * T024 (placeholder): Chat screen — root route
 *
 * Full implementation pending Phase 3 (US1).
 * Temporary placeholder until chat screen is built.
 */
import { View, Text } from "react-native";

export default function ChatScreen() {
  return (
    <View className="flex-1 bg-background items-center justify-center">
      <Text className="text-foreground text-lg">Chat Screen — Coming Soon</Text>
    </View>
  );
}
